Changes needed:
RouteSchema interface — add an optional operationId?: string field alongside summary and description.
OpenApiService.registerPath() — forward schema.operationId into the this.registry.registerPath() call, same pattern as description:
   ...schema.operationId && { operationId: schema.operationId }
Usage:
   router.get("/:id", {     operationId: "getUserById",     params: z.object({ id: z.string() }),     response: UserSchema,     summary: "Get user by ID",   }, handler);
Produces in OpenAPI JSON:
   { "operationId": "getUserById", "summary": "Get user by ID", ... }
It should be completely optional — if omitted, behavior is unchanged (no operationId in the output, same as today).
Let me revert the patches I made to your node_modules so they're clean.
Both files are already clean — no patches remain. Once you publish a meebo version with operationId support, you can add the operation IDs to the searches router and the OpenAPI output will have the exact names your frontend expects.